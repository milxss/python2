author = ("V. Hugo",) 
#tuple can store of min one value and should have a comma even if there's no more other values to follow
# we can store tuple in a variable
print(author)

#we can also use tuples in lists, for example
library = [author, "book", "year", "shelfs"]
print(library[0])
